# Values applied to the official Dify docker/.env file by bootstrap-runtime.sh.
# MedKernel does not commit or fork the upstream Dify Compose project.
# The displayed v1.14.0 release corresponds to upstream Git tag 1.14.0.
EXPOSE_NGINX_PORT=8090
EXPOSE_NGINX_SSL_PORT=8443
