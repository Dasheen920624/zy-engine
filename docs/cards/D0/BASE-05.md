# BASE-05 · 5 方言数据迁移骨架

> 读卡前置：[核心 CONSTITUTION](../../CONSTITUTION.md) + [D0 域简报](_brief.md)。
> 迁移来源（覆盖矩阵锚点）：核心 §12 五方言 · 详规 §7.5 数据存储与投影（L1400）· §7.6 核心表族（L1411）。

## 身份
- 卡 ID：BASE-05
- 域：D0 登录域 / 平台脊柱
- 关联场景：横切（所有落库卡的迁移规约）
- 依赖卡：无（与 BASE-01 同层；为全部落库卡提供迁移框架）
- 工作量：4d
- owner / reviewer：待派单（owner ≠ reviewer）

## 目标

交付**5 方言数据迁移骨架**：每个迁移在 h2 / postgres / oracle / dm / kingbase 均可执行且 schema 一致，强制中文注释 + 索引约束规约，CI 跑一致性测试——让国产化交付（核心 §12）从第一张表起就成立。

## 功能要求（原子可测条目）

- [ ] **FR-1 5 方言迁移框架**：Flyway 迁移在 h2 / postgres / oracle / dm（达梦）/ kingbase（人大金仓）均可执行；方言差异经统一占位/条件处理。
- [ ] **FR-2 一致性测试**：CI 对 5 方言跑同一套迁移后，断言表 / 列 / 类型 / 主键 / 唯一 / 索引一致（差异清单为空）。
- [ ] **FR-3 中文注释**：所有表 / 列带中文 `COMMENT`（核心 §14）；门禁校验缺注释的迁移。
- [ ] **FR-4 索引约束规约**：主键 / 唯一 / 外键 / 索引命名规约 + **业务表必带 `tenant_id` 索引**（呼应 BASE-01 行级作用域）。
- [ ] **FR-5 Oracle 标识符大小写**：处理 Oracle 下 `flyway_schema_history` 小写排查 + JDBC 标识符映射（运维手册已记）。
- [ ] **FR-6 可重入 + 回滚规约**：迁移幂等可重入；高风险迁移配回滚/补偿脚本规约。

## 接口契约 / 页面契约
### 接口契约
N·A —— 本卡是迁移工程骨架，无运行时 API。交付：迁移目录规约 + 方言适配工具 + CI 一致性测试脚本。

### 页面契约
N·A —— 无页面。

## 数据与迁移
- 表族：本卡不定义业务表，定义**迁移基线 V1**（骨架表：org/user/role/audit 等 D0 表的迁移由各自卡提供，本卡提供框架与规约 + 示范迁移）。
- 方言矩阵：h2（开发/测试）/ postgres（外网 SaaS）/ oracle / dm / kingbase（国产化内网）。
- 命名规约：表 `mk_<域>_<实体>`、索引 `idx_<表>_<列>`、唯一 `uk_<表>_<列>`、外键 `fk_<表>_<引用>`。
- 5 方言迁移：本卡即"5 方言"规约的定义者与守门人。

## 视角清单（11 视角逐条）
1. **产品架构**：迁移是 schema 单一源（关系库权威，核心 §7）；禁运行时建表/改表绕迁移。
2. **产品体验**：N·A —— 无页面。
3. **系统与数据架构**：5 方言 schema 一致是数据架构地基；10 万级表索引规约在此立。
4. **临床医疗安全**：N·A —— 但临床表的约束（非空/唯一/外键）经迁移强制，防脏数据致误判。
5. **知识与数据治理**：知识/版本表的唯一约束（如 `ACTIVE_AUTHORITATIVE` 唯一性，核心 §7）经迁移落地。
6. **安全合规与监管**：敏感字段加密列 / 脱敏视图的迁移规约（核心 §8）。
7. **集团化与多租户治理**：业务表必带 `tenant_id` + 索引（FR-4），租户隔离从 schema 起。
8. **集成与互操作**：N·A —— 外部系统不直连本库。
9. **运维 / SRE / 国产化**：★本卡主战场 —— 国产化栈（达梦 / 人大金仓）+ Oracle 兼容是交付硬指标；5 方言一致性 + Oracle 大小写排查（核心 §12）。
10. **质量与真实性审计**：CI 一致性测试是真实门禁（非"声称支持"）；迁移注释门禁。
11. **AI / 模型治理与可降级**：N·A —— 模型不落本库（候选表在 wave2 AIK 卡，仍走本迁移框架）。

## 适用不变量
- 命中核心约束：**§12 五方言迁移 + 国产化** · **§7 关系库权威** · **§14 中文注释** · **#7（schema 层呼应 tenant 隔离）**。
- 本卡落点：迁移框架 + CI 5 方言一致性测试 + 中文注释/索引门禁，让"支持国产化数据库"是**测得出的事实**而非声明。

## 验收 + 验证
- [ ] **AC-1（FR-1/2）**：示范迁移在 5 方言 CI 全部成功；schema 比对差异清单为空。
- [ ] **AC-2（FR-3）**：缺中文注释的迁移被门禁拒绝。
- [ ] **AC-3（FR-4）**：业务表缺 `tenant_id` 索引 / 不符命名规约的迁移被校验捕获。
- [ ] **AC-4（FR-5）**：Oracle 下 `flyway_schema_history` 小写场景迁移正常（含排查注记）。
- [ ] **AC-5（FR-6）**：迁移重复执行幂等（无重复建表错误）；回滚脚本规约校验。
- 关联 A1–A9：A6 合规运维（离线/国产化包验证）。
- T-GATE：CI 5 方言全绿 = 真实门禁。
- B0 验收：纯工程骨架，天然 B0。

## 完工证据
- 代码 permalink：迁移目录 + 方言适配工具 + CI 一致性测试脚本 + 示范 V1 迁移（×5 方言验证）。
- 测试：5 方言一致性比对报告 + 注释门禁测试 + Oracle 大小写场景测试。
- 审计员签字：@<reviewer>（owner ≠ reviewer）。

## 大卡工序（4d，后端/CI）
- PR1：迁移框架 + 命名/注释/索引规约 + 示范迁移 → AC-2/3。
- PR2：5 方言 CI 一致性测试 + Oracle 大小写处理 + 幂等回滚规约 → AC-1/4/5。
