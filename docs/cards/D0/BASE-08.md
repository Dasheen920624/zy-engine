# BASE-08 · 产品体验底座

> 读卡前置：[核心 CONSTITUTION](../../CONSTITUTION.md) + [D0 域简报](_brief.md) + [体验契约](../../EXPERIENCE_CONTRACT.md)（页面卡）。
> 迁移来源（覆盖矩阵锚点）：体验规范 §5 页面交互固定规则（L102）/ §6 大规模知识与资产体验（L121）· 详规 §1.1 页面统一结构 / §1.2.1 大规模列表交互（L69）· 核心 §16 体验契约。

## 身份
- 卡 ID：BASE-08
- 域：D0 登录域 / 平台脊柱
- 关联场景：横切（所有页面的交互底座）
- 依赖卡：[BASE-06](BASE-06.md)（前端骨架）· [BASE-10](BASE-10.md)（token）· [API-13](API-13.md)（服务端分页 API）
- 工作量：3d
- owner / reviewer：待派单（owner ≠ reviewer）

## 目标

交付**产品体验底座**：一页一目标 + 角色默认视图 + 专家模式 + 服务端分页 + 详情抽屉 + 异步导出 + 保存视图，把"页面乱样式乱"治成"每页只能从 token + 体验契约组件来"。

## 功能要求（原子可测条目）

- [ ] **FR-1 一页一目标**：页面骨架强制 ≤1 主按钮 / ≤3 默认筛选（核心 #6）；多同权主按钮被测试拒。
- [ ] **FR-2 角色默认视图**：按角色渲染首屏与默认列；**专家模式**才显技术对象（JSON/DSL/trace/模型提示词，核心 §14）。
- [ ] **FR-3 服务端分页**：大列表统一服务端分页组件（消费 [API-13](API-13.md)），**禁前端全量加载**（核心 #16）。
- [ ] **FR-4 详情抽屉**：统一 Drawer 详情组件（列表→抽屉下钻，不跳页丢上下文）。
- [ ] **FR-5 异步导出**：大数据导出走异步任务 + 下载中心（不阻塞、不前端拼大文件）。
- [ ] **FR-6 保存视图**：用户自定义筛选/列视图可保存、可复用。

## 接口契约 / 页面契约
### 接口契约
- 端点：保存视图持久化端点 + 异步导出任务端点（任务状态轮询）。
- DTO：视图定义 Record DTO；导出任务 Record DTO。
- 响应信封：`ApiResult` / `PageResult`（[API-13](API-13.md)）。
- 状态机：导出任务用**待办类**状态机（核心 §3：未读→处理中→已完成）。
- 幂等 / 错误码 / traceId：导出任务幂等键防重复触发。

### 页面契约（页面卡）
- 结构：本卡定义复用交互组件（分页表格 / 详情抽屉 / 导出 / 保存视图 / 专家模式开关），供页面卡引用。
- 主按钮 ≤1 / 默认筛选 ≤3 / 默认角色视图：本卡为强制器。
- 样式：仅引用核心 §5 token + 体验契约组件（禁硬编码）。

## 数据与迁移
- 表族：`sys_saved_view`（用户视图）；`sys_export_task`（异步导出任务）。
- 主键：ULID；唯一约束：`(user_id, view_name, page_key)`；索引：`user_id`、任务 `status`。
- 组织字段：带 `tenant_id`；审计字段齐全。
- 5 方言迁移：h2/postgres/oracle/dm/kingbase + 中文注释。

## 视角清单（11 视角逐条）
1. **产品架构**：体验组件单一源；页面卡禁自造表格/抽屉/导出。
2. **产品体验**：★本卡主战场 —— 一页一目标 + 角色视图 + 专家模式 + 大列表交互（核心 §16、体验契约）。
3. **系统与数据架构**：服务端分页（游标/total_estimate，详 API-13）+ 异步导出，支撑 10 万级列表。
4. **临床医疗安全**：N·A —— 但临床页默认视图低打扰（不堆同权按钮干扰医生，核心 §6）。
5. **知识与数据治理**：大规模知识/资产列表交互统一（详规 §1.2.1），知识审核台复用。
6. **安全合规与监管**：导出走审计（[BASE-04](BASE-04.md)）+ 脱敏；无权限列不渲染。
7. **集团化与多租户治理**：保存视图随租户隔离；多维治理切片默认 ≤3 维（核心 §9）。
8. **集成与互操作**：N·A。
9. **运维 / SRE / 国产化**：异步导出不拖垮服务；老年模式无障碍（核心 §12 / §5）。
10. **质量与真实性审计**：★禁前端全量加载、禁 JSON 裸渲染、禁技术词暴露（核心 #16/§14，门禁 INFRA-01）。
11. **AI / 模型治理与可降级**：AI 内容标识 + 来源摘要展示组件（核心 §6/§9）骨架，D3/D4 消费。

## 适用不变量
- 命中核心约束：**#6 一页一目标** · **#16 体验契约（服务端分页/低打扰）** · **§14 专家模式藏技术对象** · **#8 敏感导出脱敏审计**。
- 本卡落点：一套分页/抽屉/导出/保存视图/专家模式组件 + 默认角色视图，让每页样式与交互只能从 token + 契约组件来，无处可乱编。

## 验收 + 验证
- [ ] **AC-1（FR-1）**：页面出现第 2 个同权主按钮 / 第 4 个默认筛选 → 测试/评审拒。
- [ ] **AC-2（FR-2）**：普通角色看不到 JSON/DSL/trace；切专家模式才显（断言默认隐藏）。
- [ ] **AC-3（FR-3）**：万行列表请求仅取一页（服务端分页），前端全量加载被测试捕获拒绝。
- [ ] **AC-4（FR-5）**：触发大导出 → 异步任务 + 下载中心取件 + 审计；重复触发幂等。
- [ ] **AC-5（FR-6）**：保存自定义视图后重登可复用。
- 关联 A1–A9：横切（知识/质控/审计页均复用）。
- T-GATE：前端门禁全绿（无前端全量加载 / 无 JSON 裸渲染 / 无技术词暴露）。
- B0 验收：纯前端 + 确定性后端，天然 B0。

## 完工证据
- 代码 permalink：分页表格 / 详情抽屉 / 异步导出 / 保存视图 / 专家模式开关 / `sys_saved_view`+`sys_export_task` 迁移。
- 测试：一页一目标约束测试 + 专家模式隐藏测试 + 服务端分页测试 + 异步导出幂等测试。
- 审计员签字：@<reviewer>（owner ≠ reviewer）。

## 大卡工序（3d，前端为主 + 后端导出）
- PR1：分页表格 + 详情抽屉 + 一页一目标 + 专家模式 → AC-1/2/3。
- PR2：异步导出 + 保存视图 + 后端任务/视图持久化 → AC-4/5。
