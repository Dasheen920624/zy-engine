# SVC-COMPLIANCE-01 · 身份安全服务包

> 读卡前置：[核心 CONSTITUTION](../../CONSTITUTION.md) + [D5 域简报](_brief.md)。
> 迁移来源（覆盖矩阵锚点）：详规 S14 用户权限与合规 · 核心 §6 安全 · §9 多租户隔离。

## 身份
- 卡 ID：SVC-COMPLIANCE-01（服务包卡；编排 D0 身份脊柱）
- 域：D5 合规运维
- 关联场景：S14 用户、权限与合规
- 依赖卡：[AUTH-01](../D0/AUTH-01.md) 身份 · [BASE-02](../D0/BASE-02.md)/[INFRA-05](../D0/INFRA-05.md) 权限 · [SYS-06](SYS-06.md) 数据权限 · 页 [USERS-01](USERS-01.md)/[IDBIND-01](IDBIND-01.md)/[SECBASE-01](SECBASE-01.md)
- 工作量：5d
- owner / reviewer：待派单（owner ≠ reviewer）

## 目标
把**用户 + 身份绑定 + 数据权限 + 租户隔离 + 安全基线**编排为身份安全服务包：管理员可管用户/角色/权限、配身份绑定、设数据权限与安全基线，**严格租户隔离、不重造身份引擎**。

## 现状（搬迁时核查 2026-05-30，以 `medkernel-backend` 为准）
底座在 D0（编排为主）：`engine/security/` 已有 `AuthController/Service`、`CredentialAdminController/Service`、`EffectivePermissionService`、`PermissionEvaluator`、`MenuPermissionCatalog`、`ProvisionTenant*`（[AUTH-01](../D0/AUTH-01.md)/[BASE-02](../D0/BASE-02.md)/[INFRA-05](../D0/INFRA-05.md) 归属）。本卡＝把用户/身份绑定/数据权限/安全基线编排为前端可消费的服务包，**不重定义身份引擎**。

## 功能要求（原子可测条目）
- [ ] FR-1 用户管理：管理员管用户/角色/状态（复用 [BASE-02](../D0/BASE-02.md)），按租户隔离。
- [ ] FR-2 身份绑定：配置/解绑外部身份（SSO/工号），绑定可审计。
- [ ] FR-3 数据权限：配行/列数据权限（[SYS-06](SYS-06.md)），下级不可放大。
- [ ] FR-4 安全基线：配安全基线（口令策略/会话/IP 等），高危项护栏。
- [ ] FR-5 租户隔离：所有操作严格 `OrgContext` 隔离，跨租户不可见不可改。

## 接口契约 / 页面契约
### 接口契约（引擎/API 卡）
- 复用 D0 身份/权限端点（`engine/security`）+ 本卡编排端点 `GET/POST /api/v1/compliance/identity-bindings` · `GET/PUT .../security-baseline`
- 状态机：配置类（安全基线）；身份绑定变更类；幂等 + trace（[OBS-01](../D0/OBS-01.md)）

## 数据与迁移
- 复用 D0 用户/角色/权限表 + 身份绑定/安全基线表（组织字段 + 审计）；五方言（[BASE-05](../D0/BASE-05.md)）

## 视角清单（11 视角逐条）
1. 产品架构：身份安全管理的服务编排（脊柱在 D0）。
2. 产品体验：管理页清晰（页 [USERS-01](USERS-01.md)/[IDBIND-01](IDBIND-01.md)）。
3. 系统与数据架构：用户 10万级；权限求值高效（复用 [INFRA-05](../D0/INFRA-05.md)）。
4. 临床医疗安全：N·A（身份管理）。
5. 知识与数据治理：N·A。
6. 安全合规与监管：★身份/绑定/权限变更全审计（[BASE-04](../D0/BASE-04.md)）；安全基线护栏。
7. 集团化与多租户治理：★严格租户隔离，下级不可放大权限/关安全红线。
8. 集成与互操作：身份绑定对接外部 SSO/工号。
9. 运维 / SRE / 国产化：安全基线国产化合规。
10. 质量与真实性审计：★权限真实生效、租户隔离不可绕、不前端兜底放行。
11. AI / 模型治理与可降级：N·A。

## 适用不变量
- 命中核心约束：**核心 §6 安全** · **§9 多租户隔离** · **铁律 #1** · **依赖 D0 身份脊柱**。
- 本卡落点：用户/身份绑定/数据权限/安全基线编排，脊柱归 D0、不重造。

## 验收 + 验证
- [ ] AC-1（FR-1/2）：用户/身份绑定管理真实、可审计。
- [ ] AC-2（FR-3/4）：数据权限/安全基线生效、护栏不可绕。
- [ ] AC-3（FR-5）：租户隔离严格、跨租户不可见。
- 关联 A1–A9 剧本：A1 身份 · A9 合规。
- T-GATE：后端真实性门禁全绿（权限真实/隔离不可绕）。
- B0 验收：身份安全确定性，关模型可用。

## 大卡工序（5d）
- PR1：用户管理编排 + 租户隔离 + 门禁 → 验收
- PR2：身份绑定 + 数据权限（[SYS-06](SYS-06.md)）→ 验收
- PR3：安全基线 + 护栏 → 验收

## 完工证据
- 代码 permalink：身份安全服务包（编排 `engine/security`）。
- 测试：用户/绑定/数据权限/隔离/基线 + 安全测试。
- 审计员签字：@<reviewer>（owner ≠ reviewer）。
