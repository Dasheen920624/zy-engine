# 施工卡迁移 · 会话接力交接（_HANDOFF）

> **用途**：中断续接神谕。任何会话/agent 开工**第一件事读本文件**，直接拿到"现在到哪、下一步做什么"，**不要翻 transcript/git 考古**（贵且慢）。
> **维护**：每完成一批卡或一个 PR、或预感会话要中断时，**立刻更新**下面「当前活跃工作」「下一步」两段。本文件随各 PR 进 main，是逐域搬卡进度的真相源（与 backlog 互补：backlog 记任务清单，本文件记"搬卡进度 + 下一步动作 + 活跃分支"）。

## 域搬迁进度

| 域 | 卡数 | 状态 | PR |
|---|---|---|---|
| D0 登录域/平台脊柱 | 28 | ✅ 已合并 | #152 + #153 |
| D1 工作台 | 3（INFRA-09 + WORKBENCH-01/02）| ✅ 已合并 | #154 |
| D2 试点准备 | 30（23 原 ID + 7 页面）| 🚧 进行中（分 6 批 B1–B6）| — |
| D3 临床运行 | 14 + 7 页 | ⬜ 未开始 | |
| D4 质控改进 | 8 + 6 页 | ⬜ | |
| D5 合规运维 | 5 + 6 页 | ⬜ | |
| D6 高级工具 | 1 + 5 页 | ⬜ | |
| 第二波 wave2 | 55 | ⬜ | |
| GA 验收 ga | 12 | ⬜ | |

## 当前活跃工作

- **分支**：`claude/d2-foundation`（基于含 #154 的新 main）
- **批次**：D2 **B1 · 模型与版本框架基石**
- **本批 4 卡**：API-01 标准上下文 API · OPT-01 标准临床模型与 FHIR R4/R5 门面 · SYS-04 版本继承与发布框架 · SYS-08 权威知识版本解析与原子替换框架
- **状态**：分支已起；`docs/cards/D2/_brief.md` 与 4 卡**待建**
- **最近 PR**：#154（D1，已合并）

## 下一步（精确动作）

1. 建 `docs/cards/D2/_brief.md`（D2 域简报：配置类引擎做成 B0 真实、供 D3 消费；13 角色子集；依赖 D0 脊柱；域级验收＝导入/配规则路径知识字典→7 步流→灰度→全量→回滚全 B0 可跑）
2. 建 B1 四卡：**先 `grep`/`find` 核 `medkernel-backend/src` 真实现状**（标准模型 SYS-01 已建情况、FHIR 门面、版本/发布框架现状；**别信单次 read，吸取 D1 幻觉教训**），再按 [_template](_template.md) + D0 样板 [BASE-01](D0/BASE-01.md) 建卡
3. 回填 `_index.md`（D2 段 B1 卡链接 + 相关 S 场景）、`_coverage-matrix.md`（标迁 B1 命中的旧锚点 + `## 进度` 段）
4. backlog D2 段：B1 卡加链接/标注（仿 D1 的 🗂️ 行）
5. 提交 → 推送 → 开 PR（标题如 `docs(cards): D2-B1 模型与版本框架基石（API-01/OPT-01/SYS-04/SYS-08）`）→ 请用户合并
6. 用户合并后：**更新本文件**（B1✅、进度推进）→ 从新 main 起 `claude/d2-knowledge` 做 **B2**

## D2 分批计划（每批一分支一 PR，串行：合并后从新 main 重拉下一批）

| 批 | 主题 | 卡 |
|---|---|---|
| B1 | 模型与版本框架基石 | API-01 · OPT-01 · SYS-04 · SYS-08 |
| B2 | 知识 + 字典引擎 | KNOW-01/02 · API-03 · OPT-07 · TERM-01 · API-04 |
| B3 | 规则 + 路径引擎 | RULE-01 · API-05 · MED-C2 · PATH-01 · API-06 |
| B4 | 包发布 + 集成 | PKG-01 · API-10 · INTEG-01 · INTEG-02 |
| B5 | 试点服务包 | SVC-PILOT-01/02/03 · SVC-INTEGRATION-01 |
| B6 | D2 七页面 | 客户实施向导/租户开通/配置包中心/路径配置/规则库/字典映射/适配器中心（ID 仿 D1 实化）|

## 不变的坑与约定（每次都适用）

- **squash 合并后必须从新 `origin/main` 重拉分支**再做下一批；别在已合并的旧分支上接着堆（基点回退、重复带入）。
- **逐域/逐批一个 PR**；D2 太大拆 6 批，每批一独立分支、基于当时最新 main。
- **建卡前用 `grep`/`find` 对照真实仓库核现状**（`frontend/src`、`medkernel-backend/src`），**别信单次 read**——harness 曾返回伪造 read，D1 初稿因此整批返工。
- **页面卡 ID 实化**：backlog 占位 `D{n}-PAGE-*` → 语义 ID（D1 用了 `WORKBENCH-01/02`）。
- **分支前缀**：现状实际用 `claude/`（AGENTS.md 写 codex/ 但实际是 claude/）。
- **找散落草稿**：中断后先 `git worktree list` + 各 worktree 查未跟踪文件（整批卡草稿曾停在未提交的 worktree）。
- 五步搬迁流程详见设计规格 `docs/superpowers/specs/2026-05-30-doc-architecture-build-cards-design.md §7 P1`。

---
> 末次更新：2026-05-30 · D1 #154 已合并、D2 B1 启动
