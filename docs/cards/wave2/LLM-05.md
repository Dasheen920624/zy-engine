# LLM-05 · 全业务模型增强接入矩阵

> 读卡前置：[核心 CONSTITUTION](../../CONSTITUTION.md) + [wave2 域简报](_brief.md)。
> 迁移来源（覆盖矩阵锚点）：核心 §1.#15 全业务可增强 · 铁律 #4 B0 先于模型。

## 身份
- 卡 ID：LLM-05（= backlog `LLM-05`）
- 域：wave2（X-LLM）
- 关联场景：S15
- 依赖卡：[LLM-01](LLM-01.md)/[API-12](API-12.md)（能力码）· D2–D4 各引擎（被增强对象）
- 工作量：4d
- owner / reviewer：待派单（owner ≠ reviewer）

## 目标
建**全业务模型增强接入矩阵**：每个适用业务能力都有能力码 + B0 卡 + 接入点登记——确保「凡可增强处皆按统一方式接入、且都先有 B0」（核心 §1.#15）。

## 现状（搬迁时核查 2026-05-31）
**待建**：现 8 能力码硬编码于 [LLM-01](LLM-01.md)，未成矩阵、未覆盖全业务、无 B0 接入卡台账。本卡＝**矩阵化登记**（业务点 ↔ 能力码 ↔ B0 路径 ↔ 接入状态），不为每业务重写引擎。

## 功能要求（原子可测条目）
- [ ] FR-1 矩阵台账：列全部可增强业务点（字典/规则/路径/推荐/解释/质控/随访/护理/报告…）+ 能力码 + B0 路径 + 接入状态。
- [ ] FR-2 B0 前置校验：能力码上线前必须先有对应 B0 卡（铁律 #4 门禁）。
- [ ] FR-3 一致接入：各业务经 [API-12](API-12.md) 统一接入，不绕网关直连模型。
- [ ] FR-4 覆盖核查：矩阵覆盖度可核（缺口诚实标 `待接入`）。

## 接口契约 / 页面契约
### 接口契约（引擎/API 卡）
- 复用 [API-12](API-12.md)；本卡为登记/核查能力（矩阵查询端点）。

## 数据与迁移
- `model_enhancement_matrix`（业务点/能力码/B0 卡/状态），五方言；或文档台账 + 校验脚本。

## 视角清单（11 视角）
1. 产品架构：★全业务增强的统一接入图谱。
2. 产品体验：N·A。
3. 系统与数据架构：矩阵查询轻量。
4. 临床医疗安全：高危业务点强制人工/B0 兜底标注。
5. 知识与数据治理：增强产出统一走候选→审核链。
6. 安全合规与监管：接入点可审计。
7. 集团化与多租户治理：矩阵可按 OrgContext 启停。
8. 集成与互操作：N·A。
9. 运维 / SRE / 国产化：覆盖度报表可离线出。
10. 质量与真实性审计：★缺口诚实标，不虚报覆盖。
11. AI / 模型治理与可降级：★每接入点必有 B0；无模型矩阵照运行。

## 适用不变量
- 命中核心约束：**核心 §1.#15 全业务可增强** · **铁律 #4 B0 先于模型** · **#1 真实性**。
- 本卡落点：业务↔能力码↔B0 矩阵化登记 + B0 前置门禁 + 覆盖可核。

## 验收 + 验证
- [ ] AC-1（FR-1/4）：矩阵覆盖全可增强业务点、缺口诚实。
- [ ] AC-2（FR-2/3）：无 B0 的能力码不得上线；接入统一走网关。
- T-GATE：后端真实性门禁全绿。
- B0 验收：★矩阵内全能力码关模型可走 B0。

## 完工证据
- 代码 permalink：矩阵台账 + B0 前置校验。
- 测试：覆盖核查 + B0 门禁 + 统一接入。
- 审计员签字：@<reviewer>（owner ≠ reviewer）。
