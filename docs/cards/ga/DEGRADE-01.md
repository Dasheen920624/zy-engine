# DEGRADE-01 · 降级链端到端

> 读卡前置：[核心 CONSTITUTION](../../CONSTITUTION.md) + [GA 域简报](_brief.md)。
> 迁移来源（覆盖矩阵锚点）：核心 §11 B0 / 降级 · 详规 故障降级 · GA 门禁 6。

## 身份
- 卡 ID：DEGRADE-01（验收卡；四类降级矩阵单一归属）
- 域：GA 总验收
- 关联场景：全域故障降级
- 依赖卡：[QA-04](QA-04.md) B0 三态 · `engine/llm` ModelGateway · [SVC-COMPLIANCE-02](../D5/SVC-COMPLIANCE-02.md) Provider · [SYS-03](../D0/SYS-03.md) 图投影
- 工作量：3d
- owner / reviewer：待派单（owner ≠ reviewer）

## 目标
验收**四类降级端到端**：**模型 / Dify / 图投影 / 外部 Provider** 四类依赖**逐项关闭**后，主链路均可运行、诚实降级、双向恢复，**绝不因某依赖不可用而崩或伪造**。

## 现状（搬迁时核查 2026-05-30）
降级挂点已分散在各域（关模型 `MODEL_DISABLED`、断连 `NOT_CONNECTED`、图投影非权威）；模型网关 `engine/llm/ModelGatewayService` 在。本卡＝把四类降级编排为统一矩阵验收 + 双向（关→恢复）证据。

## 功能要求（原子可测条目）
- [ ] FR-1 模型降级：关模型 → 确定性命中 + `MODEL_DISABLED`；恢复 → 模型增强回归。
- [ ] FR-2 Dify 降级：关 Dify → 工作流/编排相关功能诚实降级、主链路不依赖。
- [ ] FR-3 图降级：关图（Neo4j）→ 走关系库权威（[SYS-03](../D0/SYS-03.md)）、图查询诚实降级。
- [ ] FR-4 外部降级：外部 Provider 断网 → `NOT_CONNECTED` + 重试/死信、不阻断主流程。
- [ ] FR-5 双向 + 组合：四类逐项与组合关闭/恢复均可运行，无 dangling、无伪造。

## 接口契约 / 页面契约
N·A —— 验收卡：逐项/组合切换依赖跑主链路。

## 数据与迁移
N·A。

## 视角清单（11 视角逐条）
1. 产品架构：故障韧性的总验证（四类降级矩阵）。
2. 产品体验：降级处诚实提示、六态。
3. 系统与数据架构：★关图走关系库权威、性能可接受。
4. 临床医疗安全：降级下红线/确定性 CDSS 照常。
5. 知识与数据治理：图为投影，关图不丢权威。
6. 安全合规与监管：降级留痕。
7. 集团化与多租户治理：降级不破坏隔离。
8. 集成与互操作：★外部断连重试降级（与 [QA-08](QA-08.md) 联动）。
9. 运维 / SRE / 国产化：★四类一键切换、双向恢复可演示。
10. 质量与真实性审计：★关依赖不崩、不伪造、无 dangling 404。
11. AI / 模型治理与可降级：★本卡即降级总验收（模型/Dify 两类）。

## 适用不变量
- 命中核心约束：**核心 §11 B0/降级** · **§7 关系库权威** · **§10 集成** · **铁律 #1/#2**。
- 本卡落点：模型/Dify/图/外部四类降级双向通过 + 诚实降级。

## 验收 + 验证
- [ ] AC-1（FR-1/2/3/4）：四类逐项关闭后主链路可运行、诚实降级。
- [ ] AC-2（FR-5）：双向恢复 + 组合关闭通过；无 dangling/伪造。
- T-GATE：前后端真实性门禁全绿。
- B0 验收：★与 [QA-04](QA-04.md) 互补——本卡覆盖四类全矩阵。
- **证据**：四类降级矩阵 + 关/恢复 traceId + 降级标记截图。

## 完工证据
- 降级矩阵验收报告 permalink + traceId + 切换录屏。
- 审计员签字：@<reviewer>（owner ≠ reviewer）。
