# D2 测试有效性 · 深度审计报告

> 审计日期：2026-05-29 · 审计人：Claude
> **审计结论：⚠️ 后端单测多数有效，但存在"固化假绿"个案；前端仅 render 冒烟，抓不住假闭环。1H / 3M。**

## 规模
后端 97 测试类 / 前端 23 测试文件（核心 `pages.smoke.test.tsx` 176 行 42 expect）。

## 十维度要点
- **后端有效性 🟡**：多数单测断言真实业务规则（如 A8 测 P0 豁免 ENG_EVAL_007、A6 ProgressorTest 测图遍历、A1 22 安全测试）——有效。
- 🔴 **D2-H-01（固化假绿）**：`ModelGatewayServiceTest:141-144` **断言伪造引文/模型版本为"正确"**（=A12-H-02），把假实现固化为回归基线。这是"全绿"假象的机制性根源。
- 🟡 **D2-M-01（前端仅冒烟）**：`pages.smoke.test.tsx` 全为 `render + toBeInTheDocument`（查标题渲染），**不验业务行为**，无法发现 catch 伪造成功、写死卡、署名造假等假闭环。前端假闭环全部逃过测试。
- 🟡 **D2-M-02（跨租户负向用例不足）**：仅 A8/部分仓库测试含 tenant-B 负向；CDSS 等缺跨租户隔离负向用例。
- 🟡 **D2-M-03（多方言烟测跳过）**：3 个 Docker 依赖烟测本机缺席跳过（与 B1-M-01 同源）。

## Findings
| Sev | ID | 一句话 | 位置 |
|---|---|---|---|
| High | D2-H-01 | 单测把伪造引文/模型版本断言为正确，固化假绿 | `ModelGatewayServiceTest.java:141-144` |
| Medium | D2-M-01 | 前端测试仅 render 冒烟，抓不住假闭环 | `pages.smoke.test.tsx` |
| Medium | D2-M-02 | 跨租户负向用例覆盖不足 | 多模块测试 |
| Medium | D2-M-03 | 多方言烟测 Docker 缺席跳过 | migration smoke |

合计：C0 H1 M3 L0

## 改造
- D2-H-01：随 A12 重写 LLM 测试（断言"无 provider→B0 诚实降级、无编造元数据"）。
- D2-M-01：前端补业务行为测试——断言"后端失败时显示错误态而非伪造成功""列表数据仅来自 query"。约 2-3 天（配合前端整改）。

## 总评
测试规模可观但**有效性参差**：后端核心算法多被真实测试，但 LLM 测试固化假绿、前端仅冒烟是两大盲区。**"测试全绿"不等于"功能为真"**——本审计正是反例集合。
