# 00 总入口与 AI 接手导航

## 0. 一句话

本项目是面向集团化医院、多院区、卫生所/站点和科室的 **医疗智能引擎平台**（路径 + 规则 + 图谱 + 标准化 + 适配器 + Dify/AI 工作流 + 质控 + 配置发布 + 前端配置/演示校验）。生产库与开发库必须严格区分：Oracle 是当前生产权威验收库，达梦 / PostgreSQL / KingbaseES 是生产交付兼容库，`LOCAL_H2_FILE` 只作为无内网数据库时的开发本地文件库。Neo4j 是图谱投影，Dify 是 AI 工作流执行目标，DB-only 模式必须独立可验收。

后续会有 **多个 AI（不同能力）和多位工程师并行开发**。本文是任何 AI、任何能力等级接手时必须先读的 **唯一入口**。

## 1. 10 分钟最小接手流程

> 严格按编号执行；任何一步不能跳过。

### Step 1（2 分钟）— 只读本文

本文已经内嵌项目边界、硬门禁、数据库分层、DoD、验证命令、任务选择和文档索引。默认不要先读其它长文档。

### Step 1.5（1 分钟，硬门禁）— 检查 develop 健康状态

**任何 AI 在 Step 2 选任务前，必须先做这一步。**

先读 [`../../ai-dev-input/00_DEVELOP_HEALTH.md`](../../ai-dev-input/00_DEVELOP_HEALTH.md)，根据状态字段决定能领什么任务：

| 状态 | 准入 |
|---|---|
| 🟢 GREEN | 按 Step 2 正常领任意"下一批可领"的任务 |
| 🟡 YELLOW | 可正常领任务，但 review 阶段必须附测试修复证明 |
| 🔴 RED | **冻结全部新业务任务**；只允许领该文件中"修复白名单"内的 `FIX-DEV-*` 任务 |

如本机能跑 PowerShell，可直接：

```powershell
.\scripts\check-develop-health.ps1
```

退出码：0=GREEN / 1=YELLOW / 2=RED / 10=哨兵与实测不一致需先更新哨兵。
**任何 RED 期间提交的非 FIX-DEV 业务 PR 将在 review 阶段被驳回。**

### Step 1.6（1 分钟，硬门禁）— 看 V3 功能矩阵确认任务落在哪个治理域

读 [`2026-05-21-功能矩阵-V3.md`](2026-05-21-功能矩阵-V3.md)，找到自己的任务对应：

1. 4 大治理域（配置治理 / 运营治理 / 用户与组织 / 系统）的哪个二级模块
2. 现有后端 endpoint prefix（避免新建重名 controller，例如 MpiController / UserSyncController 已经各有两份冲突副本）
3. 现有前端入口（避免新建重复页面）

**如果发现你的任务都不在 V3 矩阵列出的 4 大治理域中**——先发 ADR 提出新治理域，**不要直接动手**，否则会重复 80+ commit 大爆炸时各 AI 各塞各的反模式。

### Step 2（3 分钟）— 只看任务台账目标行

打开 [02_任务台账.md](02_任务台账.md)，只读三块：

- "下一批可领"。
- 你要认领的任务行。
- 该任务所在泳道的小节标题。

规则：

- 用户已指定任务编号 → 直接对号入座，记录"独占文件"和"依赖任务"。
- 用户未指定 → 从台账 **"下一批可领"** 段中按优先级取一个。
- 已被锁定/正在进行 → 换一个，不要并改。

### Step 3（4 分钟）— 按任务类型补读 1 份专项文档

先看 [`../05_AI实施手册.md`](../05_AI实施手册.md)，确认你的任务对应哪个 PR（PR-01 ~ PR-12）。然后按触发条件再读 1 份。除非任务明确跨多个域，否则不要展开阅读链。

| 任务触发条件 | 只补读这一份 |
|---|---|
| **任何实现 / 改造 / 设计任务（V2 金本位）** | [`../README.md`](../README.md) |
| 产品全貌 / 角色 / 9 项能力 / 22 不变量 / 三产品分层 / 术语 | [`../01_产品事实源.md`](../01_产品事实源.md) |
| 6 大客户验收剧本 / 端到端 storyboard / 设计抉择 | [`../02_场景剧本图.md`](../02_场景剧本图.md) |
| 设计 Token / 13 核心组件 API / 三产品密度模式 / 7 全局模式 | [`../03_设计系统.md`](../03_设计系统.md) |
| 18 个页面的精确规格（URL/布局/API/状态机/DoD） | [`../04_页面规格书.md`](../04_页面规格书.md) |
| **接下来 12 个 PR 的精确清单（独占文件/依赖/验收脚本/工时）** | [`../05_AI实施手册.md`](../05_AI实施手册.md) |
| 架构边界 / Provider / 外部系统 / 跨模块 | [05_架构总图与服务边界.md](05_架构总图与服务边界.md) |
| 后端接口 / 持久化 / 规则 / 路径 | [06_后端开发规范.md](06_后端开发规范.md) |
| 前端页面 / 原型 / 可视化 / E2E | [07_前端开发规范.md](07_前端开发规范.md) |
| 生产库/开发库 DDL、国产化、PG/Kingbase/DM | [数据库Provider与离线AI开发约定.md](数据库Provider与离线AI开发约定.md) |
| 部署、升级、回滚、离线包 | [09_内网部署与版本管理.md](09_内网部署与版本管理.md) |
| 医学/医保/质控来源审核 | [产品功能业务核查与开工清单.md](产品功能业务核查与开工清单.md) |
| 独立用户体系、外网账号、院内同步、SSO、身份联邦 | [用户体系与身份联邦详细设计方案.md](用户体系与身份联邦详细设计方案.md) |
| AI 医疗知识工厂、字典映射、Dify 调用 | [AI医疗知识工厂与字典映射方案.md](AI医疗知识工厂与字典映射方案.md) |
| 历史长文 / 早期蓝图 | 已物理删除，只能通过 git 历史溯源；当前实施依据永远是 V2 金本位与本目录工程规范 |
| 用户体系/SSO、AI 知识工厂、CDSS、智能评估、多语言、合规、数据治理、互联互通、AI 治理、客户交付、文档精简 | [`../01_产品事实源.md`](../01_产品事实源.md) + [02_任务台账.md](02_任务台账.md) + 对应专项工程文档 |

如果任务需要评估最低能力等级，只看 [03_AI能力分级与并行冲突规约.md](03_AI能力分级与并行冲突规约.md) 中对应等级表，不通读全文。

### Step 4（2 分钟）— 当前工作树自检

```powershell
git status -sb
git branch --show-current
git log -1 --pretty=format:%H%n%s
git pull --ff-only origin develop
.\medkernel-mvp\scripts\detect-db-env.ps1 -BootstrapLocal
.\medkernel-mvp\scripts\check-ai-collaboration.ps1
```

工作树有未提交改动且与本任务无关 → 不动它、不回滚、最终回复说明。
当前分支是 `main` → 立即停止，不允许领任务、开发、提交或推送；先切到 `develop` 或基于 `origin/develop` 创建任务分支。

### Step 5（硬门禁）— 生产库 / 开发库分层

开发前必须先确认当前任务运行在哪一类数据库上：

| 类型 | 允许用途 | 禁止事项 |
|---|---|---|
| 生产权威库：Oracle | 正式验收、真实落库、上线迁移、审计追责 | 不允许用 H2 结果冒充 Oracle smoke |
| 生产兼容库：达梦 / PostgreSQL / KingbaseES | 国产化和客户部署交付 DDL、迁移脚本、差异说明 | 不允许只维护 Oracle，不同步兼容库结构 |
| 开发本地库：`LOCAL_H2_FILE` | 无 Oracle/内网环境时的 AI 开发、DB-only 验证、本地文件持久化 | 不允许作为生产替代库，不允许提交本地数据文件 |
| `IN_MEMORY_DEMO` | 快速演示、纯单测兜底 | 不允许作为持久化、DDL、跨实例验收依据 |

任何新增表、索引、约束、持久化 SQL 或落库行为，都必须说明“生产库如何验收”和“开发库如何验证”。无 Oracle 时也必须跑 `LOCAL_H2_FILE`，并把生产库 smoke 标为待有环境补验。

## 2. 开发硬性 6 条（任何任务都适用）

| 编号 | 规则 | 解释 |
|---|---|---|
| H1 | **只改任务台账声明的独占文件** | 共享文件需先在台账登记申请，避免与其它 AI 冲突 |
| H2 | **不硬编码单医院 / 单院区 / 单系统厂商逻辑** | 差异沉淀为配置包、字典映射、适配器绑定、Provider |
| H3 | **Neo4j / Dify 不可用时核心路径/规则必须仍可用** | DB-only 模式必须可独立完成验收 |
| H4 | **关键医疗 / 医保 / 质控配置必须有来源追溯** | 缺来源、来源过期、未审核不得发布 |
| H5 | **临床建议必须由医生最终确认** | 系统给建议，医生做决定；阻断类规则必须院方授权 |
| H6 | **生产库和开发库分离** | Oracle/DM/PG/Kingbase 是生产交付口径；LOCAL_H2_FILE 只做开发等价验证 |

## 2.1 并发开发硬机制

为解决 AI 未认领、状态不及时、git 状态不同步的问题，任何代码、DDL、脚本、样例或核心文档改动前必须满足：

| 阶段 | 必做动作 | 阻断条件 |
|---|---|---|
| 开发前 | 确认当前分支不是 `main` + `git pull --ff-only origin develop` + `git status -sb` + `check-ai-collaboration.ps1` | 在 `main` 领任务或开发；工作树不干净且无法说明来源 |
| 认领前 | 创建并推送 `ai-dev-input/10_task_claims/active/<claim_id>.md` + `ai-dev-input/10_task_claims/active_locks/<task_id>.lock` | 没有远端 active claim 或 task lock 不允许改业务文件；同任务 lock 已存在则禁止认领 |
| 开发中 | 超过 60 分钟更新 `last_heartbeat` 并推送 | 4 小时无心跳视为可能 STALE |
| 提交前 | claim 状态、任务台账状态、review 状态、git diff 范围四项对账 | 任何一项不一致不得提交 |
| 推送后 | 再跑 `git status -sb`，把 commit hash 写回台账/claim/review | 当前任务分支或 develop 未推送成功不得口头宣布完成 |

冲突处理：

- active claim 写入范围重叠，或 `active_locks/<task_id>.lock` 冲突 → 停止开发，拆分任务、等待原任务完成或按接管规则处理。
- 当前分支是 `main`，或 PR 目标是 `main` 但 head 不是 `develop` → 停止；AI 任务只能合入 `develop`，`main` 只接受用户发布 PR。
- 发现他人未提交改动 → 不回滚、不覆盖；只处理自己的写入范围。
- claim 超过 24 小时无心跳且无提交 → 新 AI 必须创建接管 claim，记录原 claim 和接管原因，并在同一提交中接管或替换 task lock。
- 业务代码可以滞后于 claim，但 claim、review、feature acceptance 的状态不能滞后于实际进度。

## 2.2 功能验收与专业优化机制

为解决功能质量参差不齐的问题，以下功能必须建立 `ai-dev-input/13_feature_acceptance/` 验收记录：

- 客户可见页面、演示工作台、配置中心、质控看板。
- 医学、医保、质控、规则命中、路径推荐、Dify 解释。
- 数据库持久化、生产库/开发库 DDL、发布/回滚/同步、权限安全。
- 对外接口、第三方适配器、审计运维能力。

验收结果分级：

| 等级 | 含义 | 是否可对客户演示 |
|---|---|---|
| GOLD | 业务闭环、代码、测试、注释、数据库、交互均达标 | 可以 |
| SILVER | 主流程可用，有非阻断优化项 | 内部联调可以，客户演示需确认 |
| BRONZE | 原型或早期能力 | 不可以 |
| REJECTED | 不满足产品/架构/安全/医学/数据库底线 | 不可以 |

专业优化安排：

- P0/P1/P2 问题必须进入 `needs_optimization/`，并在任务台账登记后续任务。
- 医学/医保/质控功能必须有领域 Reviewer。
- 前端页面必须有前端 Reviewer 或浏览器验证证据。
- 数据库变更必须由数据库 Reviewer 检查表/字段注释、索引、约束和多数据库同步。
- 公共 Controller / Service / Provider、复杂规则、发布回滚同步、医学/医保判断必须有简洁意图注释；禁止无意义逐行注释。

## 3. 完成前 Definition of Done（统一口径）

> 本节是 **唯一权威 DoD**，其它文档若有冲突以本节为准。

### 3.1 代码与文档

- [ ] 只修改了任务台账声明的独占文件；意外改到共享文件已在最终回复中说明。
- [ ] 新增接口有契约测试（`@SpringBootTest` + MockMvc）。
- [ ] 新增配置有样例 JSON 或 API 示例。
- [ ] 新增数据库表/字段同步更新 Oracle、达梦、PostgreSQL/Kingbase、LOCAL_H2_FILE DDL（`ai-dev-input/04_database/`、`medkernel-mvp/db/{oracle,dm,postgres}/`、`src/main/resources/db/local/`）。
- [ ] 新增或修改的生产库表/关键字段有中文注释；H2 只做开发库结构等价。
- [ ] 复杂业务函数、公共 API、Provider、发布/回滚/同步、医学/医保/质控判断有简洁意图注释。
- [ ] 高风险或客户可见功能已创建 `ai-dev-input/13_feature_acceptance/` 验收记录。
- [ ] 更新本任务影响到的 README 或 docs。
- [ ] 新增中文字段、备注、消息文本时按 [编码与中文备注规范.md](编码与中文备注规范.md) 保证 UTF-8 无 BOM。

### 3.2 验证命令（按需）

| 任务类型 | 必跑命令 |
|---|---|
| 任何任务 | `.\medkernel-mvp\scripts\check-ai-collaboration.ps1`、`.\medkernel-mvp\scripts\run-tests.ps1`、`.\medkernel-mvp\scripts\build.ps1`、`git diff --check` |
| 涉及 DDL / 迁移 / 持久化 SQL / 落库行为 | 上述通用命令 + `.\medkernel-mvp\scripts\detect-db-env.ps1 -BootstrapLocal` + 无 Oracle 时启动 `LOCAL_H2_FILE` 验证 + 有 Oracle 时 `run-oracle-ddl.ps1` / Oracle smoke；DM/PG/Kingbase DDL 必须同步提交 |
| 涉及前端 | 上述通用命令 + 补读 `07_前端开发规范.md` + 浏览器手动验证 + （若有）Playwright E2E |
| 仅文档 | `verify-encoding.cmd` |

> 数据库验证：生产库真实验证读取本地凭据文件或环境变量，**禁止提交真实凭据**。若无法连接 Oracle/DM/PG/Kingbase，最终回复必须说明未验证原因、已完成的 `LOCAL_H2_FILE` 开发库验证和生产库补验计划。

### 3.3 提交与推送

- [ ] `git status -sb` 确认范围干净。
- [ ] 当前分支不是 `main`；AI 业务 PR 目标必须是 `develop`。
- [ ] `git add` **只暂存本任务相关文件**（不要 `git add -A`）。
- [ ] 提交信息：**中文短句、动词开头**，例：`增加配置包发布回滚`、`补充病历质控样例`。
- [ ] 立即 `git push` 到 `develop` 或开 PR 到 `develop`（**单任务单 push/PR**）。
- [ ] 因权限/网络无法推送 → 最终回复必须说明原因、commit hash 和替代交接方式。

### 3.4 最终回复模板

```text
已完成并验证。

主要改动：
- 文件1：简要说明
- 文件2：简要说明

验证：
- run-tests.ps1：PASS / FAIL（FAIL 时附原因）
- build.ps1：PASS / FAIL
- git diff --check：PASS / FAIL
- 开发库 LOCAL_H2_FILE：PASS / SKIPPED（说明原因）
- 生产库 smoke：PASS / SKIPPED（说明具体库和原因）

提交与推送：
- claim_id: <claim_id>
- review_id: <review_id>
- feature_acceptance_id: <id 或 N/A>
- commit hash: <短hash>
- 推送分支: origin/<branch>

剩余风险：
- ...

下一步建议：
- 任务台账中下一个可领编号是 ...
```

## 4. 红线（违反即必须停止）

| 红线 | 例子 |
|---|---|
| 不允许硬编码单医院逻辑 | `if (hospital.equals("ZYHOSPITAL"))` 形式禁止进入业务服务 |
| 不允许把 Neo4j / Dify 作为测试环境强依赖 | 测试不可在 Neo4j 关闭时失败 |
| 不允许外部系统传任意 SQL / Cypher 到核心引擎执行 | 必须走 Adapter / GraphProvider 受控接口 |
| 不允许配置静默覆盖已发布版本 | 同 `package_code + package_version` 内容不同必须返回错误 |
| 不允许关键医疗建议绕过医生确认 | 阻断类规则必须有 `action_mode + doctor_decision` 字段 |
| 不允许发布、回滚、同步绕过审计 | 必须写入 `ENGINE_AUDIT_LOG` |
| 不允许缺来源的医学/医保/质控配置发布 | review 必须返回 `MISSING_SOURCE` 阻断 |
| 不允许日志输出数据库密码 / Dify API Key / 患者完整隐私 | 日志必须脱敏 |
| 不允许回滚 / 覆盖其它 AI 未提交的工作 | 不确定的未提交改动先问用户 |
| 不允许 AI 在 main 领任务、开发或直接合并业务 PR | 所有 AI 变更先合入 `develop`；`main` 只接受 `develop -> main` 发布 PR |

## 5. 多 AI 并行三句话

1. **领任务前**：确认不在 `main`，先 `git pull origin develop`，再检查 claim/review/feature acceptance 队列，创建并推送 active claim 与 task lock；没有远端 claim + lock 不允许改业务文件。
2. **开发中**：只动 claim 和台账声明的独占文件；超过 60 分钟同步心跳；需要碰共享文件先更新 claim 并协调。
3. **完成后**：通过 review 和必要功能验收，再 commit + push，把台账/claim/review/feature acceptance 写入提交 hash 后归档。

## 6. 当用户没指定任务时的默认领取顺序

按 [`../05_AI实施手册.md §2 PR 拓扑图`](../05_AI实施手册.md#2-pr-拓扑图) 的 12 PR 顺序领取（PR-01 → PR-12）。若 12 PR 全部完成，再参考 [02_任务台账.md](02_任务台账.md) 中"下一批可领"段。

## 7. 文档地图（最少阅读优先）

> 📍 **本文件位置：** `docs/engineering/00_总入口与AI接手导航.md`  
> 📍 **金本位 5 份位置：** `docs/`（项目根）  
> 📍 **工程规范位置：** `docs/engineering/`（本目录）  
> 📍 **分支策略与发布管理：** [`docs/engineering/分支策略与发布管理.md`](分支策略与发布管理.md)（2026-05-19 启用：main/develop 双线 + stable-* tag）  
> 📍 **历史归档：** 已物理清理；溯源用 `git checkout pre-cleanup-20260518`（详见 CHANGELOG 2026-05-18 Removed 段）

```text
docs/                                    ← 项目文档中心（在项目根）
│
├── README.md                            ← 文档主入口
│
├── 01_产品事实源.md                     ┐ 默认只读（V2 金本位 5 份）
├── 02_场景剧本图.md                     │
├── 03_设计系统.md                       │ AI 接手任何任务先读 README，
├── 04_页面规格书.md                     │ 再按任务类型补读 1 份。
├── 05_AI实施手册.md                     ┘
│
├── engineering/                         ← 工程规范（与 V2 互补，本目录）
│   ├── 00_总入口与AI接手导航.md          ← 你在这里，硬门禁和 DoD
│   ├── 02_任务台账.md                    ← 任务状态唯一权威
│   ├── 03_AI能力分级与并行冲突规约.md
│   ├── 05_架构总图与服务边界.md          ← 四层架构 / Provider / 跨数据库
│   ├── 06_后端开发规范.md                ← 后端接口 / 服务 / 持久化
│   ├── 07_前端开发规范.md                ← 前端页面 / 交互 / E2E
│   ├── 08_国产化兼容性规约.md            ← OS/CPU/JDK/DB 深水区
│   ├── 09_内网部署与版本管理.md          ← 打包 / 升级 / 回滚 / 灾备
│   ├── 数据库Provider与离线AI开发约定.md ← 生产库 / 开发库 / DDL / DB-only
│   ├── 编码与中文备注规范.md             ← 编码
│   ├── 用户体系与身份联邦详细设计方案.md  ← SEC-001 详细方案
│   ├── AI医疗知识工厂与字典映射方案.md   ← AIK-* 详细方案
│   ├── 产品功能业务核查与开工清单.md     ← 医学 / 医保 / 质控核查
│   ├── smoke-plan-*.md                  ← 配置包 / 规则评估烟测计划
│   └── api-examples.http                ← 接口示例
│
└── engineering/reference-implementations/
    └── *.md                             ← AI 可复制的代码样板
```

> ⚠️ **AI 警告：** 历史归档目录已删除。如果你在旧 claim、旧 review 或 git 历史中看到早期长文，**禁止**把它作为实施依据，永远以当前 `docs/01-05` 和 `docs/engineering/` 为准。

## 8. 与其它文档的关系（仲裁顺序）

- 任何**产品体系 / 三产品分层 / 不变量 / 角色**冲突 → 以 [`../01_产品事实源.md`](../01_产品事实源.md) 为准
- 任何**剧本 / 端到端流程 / 验收点**冲突 → 以 [`../02_场景剧本图.md`](../02_场景剧本图.md) 为准
- 任何**设计 Token / 组件 / 视觉**冲突 → 以 [`../03_设计系统.md`](../03_设计系统.md) 为准
- 任何**页面 URL / API / 状态机**冲突 → 以 [`../04_页面规格书.md`](../04_页面规格书.md) 为准
- 任何**PR 顺序 / 独占文件 / 验收脚本**冲突 → 以 [`../05_AI实施手册.md`](../05_AI实施手册.md) 为准
- 任何**任务状态**冲突 → 以 [`02_任务台账.md`](02_任务台账.md) 为准
- 任何**架构 / Provider / 跨数据库**冲突 → 以 [`05_架构总图与服务边界.md`](05_架构总图与服务边界.md) 为准
- 任何**并行冲突 / 能力等级**仲裁 → 以 [`03_AI能力分级与并行冲突规约.md`](03_AI能力分级与并行冲突规约.md) 为准
- 任何**完成 DoD / 硬门禁**冲突 → 以本文第 3 节为准
- 任何与历史 claim/review/run log 中旧文档口径的冲突 → **永远以金本位为准**，历史记录只作溯源

## 9. 历史一致性约定

- 编号体系：ORG / PKG / TERM / RULE / PATH / GRAPH / DIFY / ADAPT / QC / PROV / FE / AUDIT / OPS / SEC / TEST / DOC。
- 状态机：`DRAFT → REVIEWED → PUBLISHED → SYNCED → ACTIVE → RETIRED`。
- 错误码：`SUCCESS / VALIDATION_ERROR / DATA_MISSING / CONFIG_NOT_FOUND / ENGINE_TIMEOUT / ADAPTER_TIMEOUT / DIFY_TIMEOUT / DB_ERROR / UNKNOWN_ERROR / NO_RULES_MATCHED / PENDING_MAPPING / MISSING_SOURCE`。
- 统一返回：`ApiResult { success, code, message, data, trace_id }`。
- 组织上下文 Header 全集：`X-Tenant-Id / X-Group-Code / X-Hospital-Code / X-Campus-Code / X-Site-Code / X-Department-Code / X-Org-Code`，Body 同名字段优先级最高。

## 10. 维护

本文是活文档。新增/淘汰文档、修改 DoD 命令、调整红线必须更新本文并提交。修改本文属于 `DOC-xxx` 任务，必须在最终回复中说明改动原因。
