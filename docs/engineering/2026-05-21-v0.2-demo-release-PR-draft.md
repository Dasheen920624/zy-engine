# v0.2-demo 发布 PR 草稿（develop → main）

> **归档说明**：本文是 v0.2-demo 阶段的历史文档，仅作历史证据保留，不再作为新任务入口。下一版本唯一目标是 **v1.0 GA / tag `v1.0.0`**。新任务请参考 [`AI_TEAM_PR_BACKLOG_V1.0_GA.md`](../AI_TEAM_PR_BACKLOG_V1.0_GA.md)。

> 这是 AI 准备好的发布 PR 文案。**用户**需要在 GitHub UI 选 `Compare & pull request: develop → main`，
> 然后把下面的标题与正文复制进去发起 PR。
>
> 不允许 AI 直接 `git push origin develop:main`（spec `.github/workflows/ci.yml` guard-rules § Enforce branch policy 已强制要求 develop → main 必须走 PR 路径）。

---

## PR 标题

```
release v0.2-demo: 241 commit 整合 + V3 全功能矩阵 + PR-V2-03 两段式布局恢复
```

## PR 正文（复制下方所有内容）

```markdown
# v0.2-demo · 演示版本发布

本次发布把 2026-05-19 ~ 2026-05-21 期间 241 个 commit 整合到 main，作为客户演示用 v0.2-demo 版本。

## 一、变更范围

### 1. 后端（28 个 Java 包）

新增 / 扩展模块：
- **配置治理**：`config/`（PKG-001..004）、`pathway/`（PATH-001..008、PR-V2-06/07）、`rule/`（RULE-001..008）、`graph/`（GRAPH-001..005）、`terminology/`（TERM-001/002）、`adapter/` + `interop/`（ADAPT-001、INTEROP-001）、`dify/`（DIFY-001/002）、`provenance/`（PROV-001..004、REFIT-003）
- **运营治理**：`quality/` + `Eval*`（PR-V2-11/12、EVAL-001/002）、`cdss/`（CDSS-001..003）、`knowledge/`（AIK-001..006）、`workflow/`（WF-001 ⚠️ 仍 Mock）、`notification/`（NOTIFY-001）
- **用户与组织**：`patient/` + `patientindex/`（MPI-001 ⚠️ 双副本）、`security/IdentityBinding*`（SEC-012）、`tenant/`（SEC-011）
- **系统**：`security/audit/`（SEC-010 审计防篡改链 + 密钥轮换）、`security/sso/`（SEC-007 CAS/OIDC/SAML/LDAP-AD）、`security/usersync/`（SEC-006 ⚠️ 双副本）、`system/`（OPS-001）

### 2. 前端（20 个页面模块 / 81 路由）

- **PR-V2-03 两段式布局恢复**：TopNav 简化为 Logo+面包屑+用户，SideMenu 改回 配置治理 / 运营治理 / 用户与组织 / 系统 四段式分组
- **Dashboard 重写**：彻底删除 FE-002 占位文案，改为功能矩阵看板（顶部 4 张 KPI + 4 个治理域共 24 张能力卡，每张含 READY / BETA / PENDING 状态徽标 + 链接跳转）
- **新增页面**：Quality（QualityDashboard / AlertList / EvalIndicatorSet / EvalResult / EvalReport / DeptDrillDown）、CDSS AlertFatigue、Knowledge（AIK）、Security（BaselinePage / IdentityBindingManagement）、Notification（List / Detail / Settings）、Pathway（List / Detail / Editor 含 X6 画布）、SSO Login

### 3. 工程基础设施

- **DEVELOP_HEALTH 哨兵 5 层防御**：`ai-dev-input/00_DEVELOP_HEALTH.md` + `scripts/check-develop-health.ps1` + 00 入口 Step 1.5/1.6 硬门禁 + `verify-pr.ps1` §0.5 哨兵预检 + CI 显式 mvn compile 步骤 + PR 模板 §0.1
- **业务异常体系**：新增 `common/exception/` 子包（BusinessException + 6 个具体异常 ValidationException / ConfigNotFoundException / DataMissingException / EngineTimeoutException / AdapterTimeoutException / DifyTimeoutException）
- **安全**：JWT 默认密钥已清出仓库；SecurityFilter 401 响应改用 Jackson 序列化 ApiResult；SecurityExceptionHandler 按 ErrorCode 动态选 HTTP 状态
- **多方言兼容**：ConfigPackageRepository.save 删 MySQL 专有 ON DUPLICATE KEY UPDATE，改为 SELECT→UPDATE/INSERT 二选一（5 个方言通用）
- **审计与文档**：
  - [`AUDIT-20260519-V2重构后全量代码审计.md`](docs/engineering/AUDIT-20260519-V2重构后全量代码审计.md) — 全量审计基线
  - [`AUDIT-20260520-增量.md`](docs/engineering/AUDIT-20260520-增量.md) — 80 commit 后增量审计
  - [`2026-05-21-功能矩阵-V3.md`](docs/engineering/2026-05-21-功能矩阵-V3.md) — V3 全功能映射（4 治理域 × 50 endpoint × 81 路由）
  - [`2026-05-20-破窗行动.md`](docs/engineering/2026-05-20-破窗行动.md) — 历史 RED 状态修复派单（已归档）

## 二、验证状态

| Gate | 标准 | 实测 | 通过 |
|---|---|---|---|
| 编译 | `mvn -q compile` SUCCESS | 261 源文件 PASS（仅 1 个 unchecked warning） | ✅ |
| 测试 | `mvn test` SUCCESS | 248 个 contract test 全部通过，0 failure 0 error 0 skipped | ✅ |
| 前端 | `npm run lint && typecheck && test && build` | 需 CI 验证（开发本机 Node 12 < 18 要求） | ⏳ |
| 哨兵 | `ai-dev-input/00_DEVELOP_HEALTH.md` 状态 | 🟡 YELLOW（后端绿，前端待 CI 验） | ⏳ |

## 三、⚠️ 已知缺陷（演示前请回避）

详见 [`2026-05-21-功能矩阵-V3.md`](docs/engineering/2026-05-21-功能矩阵-V3.md) §5：

| 编号 | 描述 | 演示建议 |
|---|---|---|
| KD-001 | WF-001 待办中心后端全 Mock | 演示只看不点，不演示审批/驳回/转办 |
| KD-002 | MpiController 双副本（`/api/v1/mpi` vs `/api/mpi`） | 演示前选一份，另一份在 UI 上不暴露 |
| KD-003 | UserSyncController 双副本（`security.*` vs `security.usersync.*`） | 同上 |
| KD-004 | HikariCP 未接入，16 处 DriverManager 直连 | **禁止压测**，低并发演示无影响 |
| KD-005 | Jackson SNAKE_CASE 未启用 | 演示功能不受影响 |
| KD-006 | OrgContext 双定义未完全统一 | 演示无影响 |
| KD-007 | 多入口仍是 PlaceholderPage（adapter/dify/mpi/tenant/admin） | 演示时跳过这几个菜单 |

## 四、演示剧本（建议 30 min）

1. **登录 + 切组织**（5 min）：本地账号登录 → 工作台显示 28 包/20 页/248 测试 KPI → 切到 HOSPITAL_ALPHA
2. **配置包发布闭环**（10 min）：配置包中心 → Import Wizard 5 步 → MISSING_SOURCE 阻断演示 → 补来源 → 发布
3. **路径推荐（AMI）**（5 min）：演示与校验 → AMI_RECOMMEND 剧本 → 看命中规则 + 来源卡片 + 医生确认
4. **质控驾驶舱**（5 min）：院级质控驾驶舱 → 看 KPI → 下钻到科室 → 评估指标
5. **AI 知识审核**（5 min）：AI 知识审核 → 看 Dify 生产任务 → 审核通过

## 五、回滚方案

如演示中发现致命 bug，立即：

```powershell
git checkout main
git reset --hard <上一个 release tag>
git push origin main --force-with-lease  # 需 admin 操作 GitHub UI
```

之前发布候选 tag：（暂无 v0.1-stable 标记，建议这次先 tag main 当前 HEAD 作为回滚锚）

## 六、合并后行动

- [ ] tag `v0.2-demo` 指向 main HEAD
- [ ] 触发演示环境部署（Nginx + Spring Boot jar）
- [ ] 把 `ai-dev-input/00_DEVELOP_HEALTH.md` 状态从 🟡 改 🟢
- [ ] GitHub 仓库 Settings → Branches 开启 main + develop 的 branch protection（require status checks: backend-build-test / guard-rules / Compile only）—— 没这一步还会再坏一次

---

🤖 PR 文案由 AI 在 develop @ e4ab32f 时点准备。
合并前请人工 review CI 跑通 + 演示环境 smoke 验证。
```

---

## 用户执行步骤

1. 打开 https://github.com/Dasheen920624/medkernel/compare/main...develop
2. 点 "Create pull request"
3. PR 标题填上面的"PR 标题"
4. PR 正文复制上面 ```markdown ... ``` 围栏内的全部内容
5. 等 CI 全绿（backend-build-test + guard-rules + Compile only 三个 check）
6. 在演示环境跑一次 smoke（参考 PR 正文 §四 剧本）
7. 通过后 Merge（建议用 **Create a merge commit**，保留所有历史）
8. 立即 tag `v0.2-demo` 指向 main HEAD
9. 把 `ai-dev-input/00_DEVELOP_HEALTH.md` 状态改 🟢 并 commit
10. 在 Repo Settings → Branches 把 main 设为 Protected（防止下一次破窗）
