// Auto-generated TypeScript types from OpenAPI 3.0 spec.
// DO NOT EDIT MANUALLY — regenerate with:
//   npm run gen:types          (requires running backend)
//   npm run gen:types:offline  (uses ../medkernel-mvp/target/openapi.json)
//
// Generation flow:
//   1. cd medkernel-mvp && mvn package  →  produces target/openapi.json
//   2. cd frontend && npm run gen:types:offline  →  produces this file
//
// This file is committed as a reproducibility baseline.
// Re-run generation whenever backend Controller signatures change.

export interface paths {
  // Placeholder — populated by openapi-typescript from the OpenAPI spec.
  // After running gen:types or gen:types:offline, this will contain
  // typed paths for all /api/** endpoints.
}

export interface webhooks {
  // No webhooks defined in current spec.
}

export interface components {
  schemas: {
    // Placeholder — populated by openapi-typescript.
    // Will include ApiResult, PagedResult, and all DTO schemas.
  };
}

export interface operations {
  // Placeholder — populated by openapi-typescript.
  // Will include typed operations for all Controller endpoints.
}
